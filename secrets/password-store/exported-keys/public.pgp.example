# Local-only password-store public key export (gitignored).
#
# Export your public key locally if needed:
#   gpg --armor --export <key-id> > secrets/password-store/exported-keys/public.pgp
#
# Never commit real key material — use this file as a reminder only.
