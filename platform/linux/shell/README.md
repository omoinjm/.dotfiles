# Import script

```bash
# In .bashrc
source /mnt/d/omoi/git/dotfiles/linux/shell/.bash/.bashrc

# Create symbolic link pointing to repo
ln -s /mnt/d/omoi/git/dotfiles/linux/shell/.bash/.envrc .envrc
```

Setup [`direnv`](https://direnv.net/)

