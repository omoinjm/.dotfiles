# Import script

```bash
# In ~/.bashrc
source ~/dotfiles/platform/linux/shell/bash/.bashrc

# In ~/.zshrc
source ~/dotfiles/platform/linux/shell/zsh/.zshrc

# Create a symbolic link pointing to the repo's direnv config
ln -s ~/dotfiles/platform/linux/shell/bash/.envrc .envrc
```

Setup [`direnv`](https://direnv.net/)

